def run(config_path: str = "data/survey_config.json", dry_run: bool = False):
    raise NotImplementedError(
        "single-agent architecture is not yet implemented. "
        "Run with --architecture baseline instead."
    )
